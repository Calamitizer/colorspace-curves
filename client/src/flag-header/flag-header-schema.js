(function() {
    'use strict';

    const PropTypes = require('prop-types');

    const propTypes = {
        /* */
    };

    const defaultProps = {
        /* */
    };

    const scheme = {
        propTypes,
        defaultProps,
    };

    module.exports = scheme;
}());
